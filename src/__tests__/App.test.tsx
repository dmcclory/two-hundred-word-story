import { describe, expect, it } from 'vitest'
// import { render, screen, userEvent } from './utils/test-utils'
import App from '../App'

describe('simple test!', () => {

  it(' is a test!', () => {
    const x = 300
    expect(x * 2 === 600).toBeTruthy()
  })
})
